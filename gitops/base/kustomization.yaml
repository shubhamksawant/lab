apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  # Only manage safe resources that won't interfere with running app
  # This demonstrates GitOps without breaking existing functionality

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: humor-game
