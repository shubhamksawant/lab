apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  # For demo purposes, we'll start with just the namespace
  # This demonstrates the GitOps workflow without complex resource conflicts

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: humor-game
